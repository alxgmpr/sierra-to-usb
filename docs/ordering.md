# Ordering / fab notes (draft)

Draft notes for the JLCPCB order form and fab-side settings. Expand as later
tasks (assembly, panelization) add requirements.

## Stackup

**JLCPCB "JLC7628" (JLC04161H-7628): 4-layer, 1.6mm, 1oz outer / 0.5oz inner,
7628-prepreg controlled-impedance stackup.**

| # | Layer | Type | Thickness | Material | Er |
|---|-------|------|-----------|----------|----|
| 1 | F.Cu (L1, signal) | copper | 0.035mm (1oz) | — | — |
| — | dielectric 1 | prepreg | 0.2104mm | Nan Ya NP-155F 7628 | 4.4 |
| 2 | In1.Cu (GND plane) | copper | 0.0152mm (0.5oz) | — | — |
| — | dielectric 2 | core | 1.065mm | Nan Ya NP-155F Core | 4.43 |
| 3 | In2.Cu (PWR plane) | copper | 0.0152mm (0.5oz) | — | — |
| — | dielectric 3 | prepreg | 0.2104mm | Nan Ya NP-155F 7628 | 4.4 |
| 4 | B.Cu (L4, signal) | copper | 0.035mm (1oz) | — | — |

Source: [jlcpcb.com/impedance](https://jlcpcb.com/impedance) (JLC7628 4-layer
1.6mm controlled-impedance stackup) and the JLCPCB-derived KiCad stackup
template `jlcpcb_4L_1.6mm_outer1oz_inner0.5oz_JLC04161H-7628.kicad_pcb` from
[gsuberland/jlcpcb_autogenerated_stackups](https://github.com/gsuberland/jlcpcb_autogenerated_stackups)
(auto-extracted from JLCPCB's own published per-material Er/thickness
figures). This exact `(stackup ...)` block is embedded in
`sierra-to-usb.kicad_pcb`'s Board Setup > Physical Stackup.

**Order form entries:** 4 Layer, 1.6mm board thickness, 1oz outer copper /
0.5oz (½oz) inner copper, TG155 / standard FR-4, **Impedance Control: Yes**
(select the JLC7628 / 7628-prepreg stackup option when JLCPCB's order form
prompts for the controlled-impedance layer stack).

## Controlled-impedance trace geometries

All controlled classes route on L1 (top), referenced to the In1 GND pour
immediately below through the 0.2104mm / Er 4.4 prepreg (dielectric 1).

**Geometry source (2026-07-16): JLCPCB's live impedance calculator**
([jlcpcb.com/pcb-impedance-calculator](https://jlcpcb.com/pcb-impedance-calculator)),
inputs: 4 layers / 1.6mm / inner 0.5oz / outer 1oz, results read from the
**JLC04161H-7628 (Standard, finished thickness 1.59mm ±10%)** stackup tab —
the same 7628/RC49% 0.2104mm prepreg + 1.065mm core stackup as this board.
These values replace the original Task-15 closed-form numbers and are the
figures JLCPCB's coupon-controlled impedance process will hold (±10%).

| Net class | Application | Trace width | Gap | Target Z |
|---|---|---|---|---|
| `DIFF_USB_90` | USB2 D+/D-, USB3 SuperSpeed pairs | 0.2474mm | 0.15mm | 90 Ω diff |
| `DIFF_PCIE_85` | M.2 PCIe lanes, REFCLK | 0.2835mm | 0.15mm | 85 Ω diff |
| `DIFF_MDI_100` | RTL8125 Ethernet MDI | 0.1826mm | 0.15mm | 100 Ω diff |
| `RF_50` | ANT*/GNSS, CPWG (grounded coplanar waveguide) | 0.3345mm | 0.30mm (ground gap) | 50 Ω single-ended |
| `Default` | all uncontrolled signals (user-requested 50 Ω) | 0.3586mm | — | 50 Ω single-ended |

> **RESOLVED (2026-07-16) — `RF_50` re-derivation order gate CLOSED.** The
> JLCPCB calculator confirms the closed-form suspicion: for ground-gap
> 0.30mm on this stackup, 50 Ω needs **W=0.3345mm**, not the 0.14mm the
> quasi-static CPW model produced (which would have run ~70+ Ω). Net-class
> geometry in `sierra-to-usb.kicad_pro`, the DRC rules in
> `sierra-to-usb.kicad_dru`, and the board fab-note text were all updated
> to the calculator values. `Default`-class traces additionally carry a
> relaxed DRC minimum (0.09mm fab floor) so fine-pitch neck-downs stay
> legal; the four controlled classes remain hard-constrained.

Copper-to-other-net clearance floor for the four controlled classes: **0.30mm** (matches
the RF_50 CPWG's own designed ground-fill gap; gives the diff-pair classes
~1.4x the reference-plane height as crosstalk isolation margin). These
numbers are entered as KiCad net-class defaults (`sierra-to-usb.kicad_pro`)
and enforced as hard DRC minimums in `sierra-to-usb.kicad_dru`.

When placing the JLCPCB order, request these exact width/gap/clearance
figures be held (JLCPCB's impedance-control order option lets you attach a
target table); note the stackup name/dielectric figures above so their
engineering team matches the same prepreg lot assumptions used in this
derivation.

## Trace current capacity (pre-defined width ladder)

The pre-defined track widths in `sierra-to-usb.kicad_pro` carry these
IPC-2221 external-layer ampacities (1oz outer copper, I = 0.048·ΔT^0.44·A^0.725
— the conservative baseline; IPC-2152 with the In1/In2 planes as heat
spreaders allows more, so treat these as safe floors):

| Width | 10°C rise | 20°C rise | Intended use |
|---|---|---|---|
| 0.5mm | 1.45 A | 1.96 A | light power spurs |
| 0.8mm | 2.03 A | 2.76 A | 3V3 branch feeds |
| 1.0mm | 2.39 A | 3.24 A | 3V3 rails (≤2A) |
| 1.5mm | 3.21 A | 4.35 A | 5V VBUS (3A) |
| 2.0mm | 3.95 A | 5.36 A | 12V PoE-secondary trunk (~2A, 2x margin) |
| 3.0mm | 5.30 A | 7.20 A | bulk — prefer pours |

Vias (JLCPCB 18µm avg barrel plating, ΔT 10°C): 0.25 drill ≈ 1.3A,
0.3 ≈ 1.5A, 0.4 ≈ 1.8A, 0.6 ≈ 2.4A each — use ≥2 of 0.8/0.4 per layer
transition on any 3A path. Impedance trace widths are NOT sized for power;
controlled-class nets carry signals only.

## PoE primary-domain isolation

The PoE primary/hazardous-voltage domain (`POE_PRI` net class: POE_VA*,
POE_VB*, POE_VDD54*, POE_VSS, POE_RTN, plus the TPS23730/flyback-primary
control nets) must keep **≥2.5mm copper clearance** from all other-net
copper everywhere on the board, enforced via a custom DRC rule in
`sierra-to-usb.kicad_dru`. See the "POE_PRI_MOAT" rule-area zone and its
Cmts.User text note in the PCB for the placeholder isolation region (the
human router resizes it to enclose the placed primary-side components).
